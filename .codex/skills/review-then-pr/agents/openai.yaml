interface:
  display_name: "Review Then PR"
  short_description: "Review the current change, then open a PR only when findings are empty"
  default_prompt: "Use $review-then-pr to review the current change and open a ready-for-review PR only if the review returns no actionable findings, unless the user explicitly asks for draft."
